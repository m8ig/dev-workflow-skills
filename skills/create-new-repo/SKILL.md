---
name: create a new repo
description: Use when initializing a new git repository. Covers default branch naming, .gitignore, and initial project setup conventions.
---

Always set the name of the default branch to `master` instead of `main`.
